<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>

   <span>{% if isAdminLogin %}ADMIN {% endif %}{{ loginHeaderText }}</span>
    <form>
      <div>
         <input type="text" name="username" required/>
      </div>
      <div>
         <input type="password" name="password" required/>
      </div>
      {{ submitButtonHTML }}
    </form>
   <span>{{ totalSiteLogins }}</span>
</body>

</html>